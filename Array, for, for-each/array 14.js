const numbers = [1, 2, 3, 4, 5];
    let sum = 0;
    // Цикл проходит по индексам массива
    for (let i = 0; i < numbers.length; i++) { 
        if (i % 2 !== 0) {  // Условие проверяет: индекс нечётный?
            sum += numbers[i]; // Складываются элементы с нечётными индексами
        }
    }
    console.log(sum); // Вывод: 6
